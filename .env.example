# Optional — lib/supabase.ts already defaults to the project below, so the site
# works with no .env file at all. Set these only to point at a different
# Supabase project (staging, a fork, a rotated key).
# Copy to .env.local (gitignored) and restart `npm run dev`.
SUPABASE_URL=https://kjadyhxrogpovpjpbcea.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_SIrM1Wrp7xBXihJZfL2vtQ_vYULQIH-
